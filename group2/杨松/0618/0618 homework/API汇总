字符串API :
charCodeAt();返回索引对应的编码
fromCharCode();由编码返回对应字符串
indexOf();指定值的第一次出现的索引; 如果没有找到 -1。
lastIndexOf();返回指定值在调用该方法的字符串中最后出现的位置，如果没找到则返回 -1。
padEnd();在原字符串末尾填充指定的字符串直到目标长度所形成的新字符串。
padStart();在原字符串开头填充指定的字符串直到目标长度所形成的新字符串。
repeat();指定字符串的指定数量副本的新字符串。接收一个参数，值为多少复制多少次
split();分割
substring();返回一个从原字符串中提取出来的新字符串；如果接收的参数为负数，把负数变成0
slice();返回一个从原字符串中提取出来的新字符串，如果接收的参数为负数，从右到左
toLocaleLowerCase();转换为小写字符串
toLocaleUpperCase();转换为大写字符串
toString();神奇的toString,返回指定对象的字符串形式，也可以进行进制转换，接收参数是多少就转为多少进制;
trim();删除字符串两端的空白字符
...

Math :
Math.min();
Math.max();
Math.floor();
Math.round();
Math.random();
Math.PI;
Math.cos();
...


Date ：日期对象；内置对象

getFullYear()     年
getMonth()        月
getDate()         日
getDay()          天(周几)
getHours()        时
getMinutes()      分
getSeconds()      秒
getMilliseconds() 毫秒
...

BOM 浏览器对象模型
window => 所有顶层数据，包括全部的全局变量；window把浏览器的功能划分成了很多子对象
window.alert();
window.onload();
window.history();
window.location();
history.back(); 向后
history.forword();向前
history.go(-1);向后
history.go(1);向前
...

location :
http:// www.baidu.com?key=value#haxizhi
协议      域名         查询语句   哈希值

hash ：跨页面传值